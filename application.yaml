apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: maksymshemet-blog-smv-pp-ua
  namespace: blog
spec:
  project: default

  source:
    repoURL: https://github.com/maksimshemet/devops-blog.git
    targetRevision: HEAD
    path: prod
  destination: 
    server: https://kubernetes.default.svc
    namespace: blog

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true  
